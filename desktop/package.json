{
  "name": "freellm-studio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build",
    "build:debug": "tauri build --debug --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
